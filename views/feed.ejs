<%- include('partials/headerAuth') -%>

<div id="main" class="wrapper style1">
  <div class="container">
    <header class="major">
      <h1>Concerts</h1>
    </header>
    <div class="">
      <div class="shows">
          <% for (let i = 0; i < shows.length; i++) { %>
            <div class="">
              <h3><%= shows[i].artist %></h3>
              <p>
                <a href="/show/<%= shows[i]._id %>" class="image fit">
                  <img src="<%= shows[i].image %>" alt="">
                </a>
              </p>
            </div>
          <% } %>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') -%>